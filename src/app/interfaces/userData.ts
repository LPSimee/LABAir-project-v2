export interface UserData {
    nome: string;
    cognome: string;
    email: string;
    password: string;
    giorno: string;
    mese: string;
    anno: string;
}

export interface LoginData {
    email: string;
    password: string;
}